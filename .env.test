# .env.test
# E2Eテスト専用環境変数（CI と完全に同じ値）
# 使用方法: E2E実行前に .env を .env.backup にリネームし、これを .env にコピー

# Application
NODE_ENV=test
APP_PORT=3000

# Security
# ⚠️ IMPORTANT: test-helpers.ts:31 と完全に一致させること
ADMIN_TOKEN=test-admin-token
JWT_SECRET=test-jwt-secret
JWT_EXPIRES_IN=900s
REFRESH_SECRET=test-refresh-secret
REFRESH_EXPIRES_IN=30d
REFRESH_ROTATE=true
# ⚠️ Buffer.from('test-pepper').toString('base64') の結果
SECURITY_PIN_PEPPER=dGVzdC1wZXBwZXI=

# Database (MySQL)
# ⚠️ IMPORTANT: test-helpers.ts:22 は 127.0.0.1 を使用
DB_TYPE=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=reserve_user
DB_PASSWORD=reserve_password_change_me
DB_DATABASE=reserve_db
DB_SYNCHRONIZE=true
DB_LOGGING=false

# MySQL Docker (for docker-compose)
MYSQL_ROOT_PASSWORD=root_password_change_me
MYSQL_DATABASE=reserve_db
MYSQL_USER=reserve_user
MYSQL_PASSWORD=reserve_password_change_me
MYSQL_PORT=3306

# phpMyAdmin (optional, dev only)
PHPMYADMIN_PORT=8080

# Timezone
TZ=Asia/Tokyo
